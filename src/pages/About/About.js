import React, { useEffect, useState } from "react";
import { Link, useLocation } from "react-router-dom";
import Breadcrumbs from "../../components/pageProps/Breadcrumbs";

const About = () => {
  const location = useLocation();
  const [prevLocation, setPrevLocation] = useState("");
  useEffect(() => {
    setPrevLocation(location.state.data);
  }, [location]);
  return (
    <div className="max-w-container mx-auto px-4">
      <Breadcrumbs title="About" prevLocation={prevLocation} />
      <div className="pb-10">
        <h1 className="max-w-[600px] text-base text-lightText mb-5">
          <span className="text-primeColor font-semibold text-lg">
            Kortex
          </span>{" "}
           we offer a curated selection of the latest gadgets, trendy
          clothes, stylish accessories, and quality furniture and home
          appliances. Whether you're looking for a premium brand-new item or a
          high-quality pre-owned find, we've got you covered with a wide range
          of options at unbeatable prices.
        </h1>
        <Link to="/shop">
          <button className="w-52 h-10 bg-primeColor text-white hover:bg-black duration-300">
            Continue Shopping
          </button>
        </Link>
      </div>
    </div>
  );
};

export default About;
